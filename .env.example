# Local API preview configuration.
# Copy this to .env.local and fill with your real server-side values.

SONIC_ARTIST_EVENTS_ENABLED=true
SONIC_GOABASE_ENABLED=true
SONIC_GOABASE_LIST_LIMIT=40
SONIC_GOABASE_DETAIL_LIMIT=20
SONIC_GOABASE_TIMEOUT_MS=8000
SONIC_EVENT_RADAR_ENABLED=true
SONIC_EVENT_RADAR_DAILY_LIMIT=160
SONIC_EVENT_RADAR_LIMIT=50
SONIC_TICKETMASTER_ENABLED=true
TICKETMASTER_CONSUMER_KEY=
TICKETMASTER_CONSUMER_SECRET=
SONIC_BANDSINTOWN_ENABLED=true
BANDSINTOWN_APP_ID=

# Optional aliases also supported by the app:
# TICKETMASTER_API_KEY=
# TICKETMASTER_DISCOVERY_API_KEY=
# TM_CONSUMER_KEY=
# TM_CONSUMER_SECRET=

# Compliance-first optional integrations.
# Public, no-secret APIs below are enabled by default with cache, limits and User-Agent.
# Credentialed providers below are active when official project credentials are present.
SONIC_NEWS_FEED_ENABLED=true
SONIC_NEWS_FEED_ROUTE_ENABLED=true
SONIC_NEWS_USER_AGENT=SonicSearch/1.0 (+https://sonicsearch.app)
SONIC_NEWS_FEED_CACHE_SECONDS=900
SONIC_NEWS_FEED_DAILY_LIMIT=80
SONIC_NEWS_FEED_TIMEOUT_MS=5200

# Optional durable usage store for daily API limits and one-per-user AI image locks.
# Without these, development falls back to in-memory limits.
# In production, keep SONIC_REQUIRE_DURABLE_RATE_LIMITS=true and configure KV/Upstash.
KV_REST_API_URL=
KV_REST_API_TOKEN=
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
SONIC_REQUIRE_DURABLE_RATE_LIMITS=true
SONIC_API_MAX_BODY_BYTES=1048576
SONIC_NATIVE_APP_ALLOWED_ORIGINS=capacitor://localhost,ionic://localhost
SONIC_AI_ALLOWED_ORIGINS=https://sonicsearch.app,https://www.sonicsearch.app,capacitor://localhost,ionic://localhost
SONIC_MUSIC_ALLOWED_ORIGINS=https://sonicsearch.app,https://www.sonicsearch.app,capacitor://localhost,ionic://localhost

# Social login. The iOS Capacitor app uses an external secure browser and
# returns through this custom scheme; add the same URL to Supabase Auth redirect URLs.
SONIC_SOCIAL_ALLOWED_REDIRECTS=sonicsearch://auth/callback

# Private beta. The public page remains visible; the full app validates invite
# codes through /api/beta-access so production codes do not live in index.html.
SONIC_BETA_ALLOWED_ORIGINS=https://sonicsearch.app,https://www.sonicsearch.app
SONIC_BETA_ACCESS_CODES=
SONIC_BETA_ACCESS_SECRET=
SONIC_BETA_ACCESS_TTL_DAYS=30
SONIC_BETA_ACCESS_ATTEMPT_LIMIT=20
SONIC_BETA_ACCESS_ATTEMPT_WINDOW_SECONDS=900
# Optional hard gate for private beta APIs. When true, protected backend
# routes require the signed beta token issued by /api/beta-access.
# Production defaults to requiring beta access when this is unset.
SONIC_REQUIRE_BETA_ACCESS_FOR_APIS=true
# Leave false for browsers; native Capacitor origins receive a bearer token automatically.
SONIC_BETA_ACCESS_RETURN_TOKEN=false
SONIC_WAITLIST_ENABLED=true
SONIC_WAITLIST_DAILY_LIMIT=24
SONIC_BETA_EVENTS_ENABLED=true
SONIC_BETA_EVENTS_DAILY_LIMIT=300

# Social/community anti-abuse limits.
SONIC_SOCIAL_COMMENTS_ENABLED=true
SONIC_SOCIAL_COMMENTS_DAILY_LIMIT=160
SONIC_COMMUNITY_ENABLED=true
SONIC_COMMUNITY_DAILY_LIMIT=120
SONIC_CATALOG_EXTRA_ROUTE_ENABLED=true
SONIC_CATALOG_EXTRA_DAILY_LIMIT=240
SONIC_SOCIAL_CONFIG_ROUTE_ENABLED=true
SONIC_SOCIAL_OAUTH_ROUTE_ENABLED=true

# Server-side track metadata/previews. iTunes Search is public/documented; Deezer stays opt-in.
SONIC_TRACK_METADATA_ENABLED=true
SONIC_DEEZER_ENABLED=false
SONIC_ITUNES_ENABLED=true
SONIC_TRACK_METADATA_DAILY_LIMIT=120
SONIC_TRACK_METADATA_CACHE_SECONDS=3600

# Approval-sensitive search/playback APIs. They become active when official
# project credentials are present in the backend environment.
SONIC_SPOTIFY_ENABLED=true
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=
SONIC_SPOTIFY_SEARCH_DAILY_LIMIT=80
SONIC_SPOTIFY_SEARCH_LIMIT=8
SONIC_SPOTIFY_MARKET=BR
SONIC_SPOTIFY_MIN_CONFIDENCE=0.72

SONIC_SOUNDCLOUD_ENABLED=true
SOUNDCLOUD_CLIENT_ID=
SOUNDCLOUD_CLIENT_SECRET=
SONIC_SOUNDCLOUD_SEARCH_DAILY_LIMIT=80
SONIC_SOUNDCLOUD_SEARCH_LIMIT=12
SONIC_SOUNDCLOUD_PREVIEW_RESOLVE_LIMIT=12

SONIC_YOUTUBE_ENABLED=true
YOUTUBE_API_KEY=
YOUTUBE_DATA_API_KEY=
SONIC_YOUTUBE_SEARCH_DAILY_LIMIT=80
SONIC_YOUTUBE_SEARCH_LIMIT=5
SONIC_YOUTUBE_QUERY_VARIANTS_LIMIT=2

# Server-side cover art. Uses MusicBrainz + Cover Art Archive with User-Agent, cache and daily limit.
SONIC_COVER_ART_ENABLED=true
SONIC_COVER_ART_DAILY_LIMIT=100
SONIC_COVER_ART_CACHE_SECONDS=86400
SONIC_COVER_ART_TIMEOUT_MS=9000

# Server-side artist profile enrichment with attribution-friendly sources.
SONIC_ARTIST_PROFILE_ENABLED=true
SONIC_MUSICBRAINZ_ENABLED=true
SONIC_WIKIPEDIA_ENABLED=true
SONIC_ARTIST_PROFILE_DAILY_LIMIT=80
SONIC_ARTIST_PROFILE_CACHE_SECONDS=86400
SONIC_REFERENCE_USER_AGENT=SonicSearch/1.0 (+https://sonicsearch.app)

# Optional artist/social taste enrichment. Last.fm needs your own free API key.
SONIC_LASTFM_ENABLED=true
LASTFM_API_KEY=
SONIC_LASTFM_DAILY_LIMIT=80
SONIC_LASTFM_CACHE_SECONDS=86400

# Public internet radio directory by style. No key required; keep backend/cache/limits on.
SONIC_RADIO_BROWSER_ENABLED=true
SONIC_RADIO_BROWSER_DAILY_LIMIT=120
SONIC_RADIO_BROWSER_CACHE_SECONDS=21600
SONIC_RADIO_BROWSER_LIMIT=8

# AI text/image and artist references run only through the backend.
# Discogs is called only when a project token is present.
SONIC_AI_TEXT_ENABLED=true
SONIC_ARTIST_BIO_ENABLED=true
SONIC_AI_BIO_DAILY_LIMIT=18
SONIC_AI_TRACK_DAILY_LIMIT=24
SONIC_AI_NEWS_DAILY_LIMIT=24
SONIC_AI_IMAGE_ENABLED=true
SONIC_AI_IMAGE_DAILY_LIMIT=25
SONIC_AI_IMAGE_REQUIRE_PREMIUM=false
SONIC_SPIRIT_IMAGE_REQUIRE_TRUSTED_USER=false
SONIC_AI_IMAGE_STORE_REQUIRED=true
SONIC_AI_IMAGE_ALLOW_BETA_REGENERATION=false
SONIC_AI_IMAGE_MAX_PER_USER=1
SONIC_ARTIST_BIO_REFERENCE_CACHE_SECONDS=86400
SONIC_DISCOGS_ENABLED=true
DISCOGS_USER_TOKEN=
# Optional aliases:
# DISCOGS_TOKEN=
# SONIC_DISCOGS_USER_AGENT=SonicSearch/1.0 (+https://sonicsearch.app)

# Client-side Deezer/Wikipedia/MusicBrainz hydration stays disabled in index.html by default.
# iTunes Search may run in the client because Apple documents JSONP use for websites.
# Prefer backend routes for any provider that needs credentials, approval, custom User-Agent or stricter attribution.

# Supabase/Postgres user database.
# Keep service-role keys out of frontend. Use service role only as backend/server env.
SUPABASE_URL=
# Optional branded Supabase Auth domain for Google OAuth.
# Example: https://auth.sonicsearch.app
# Configure it in Supabase Custom Domains and add its /auth/v1/callback URL
# to Google Cloud before enabling it in production.
SUPABASE_AUTH_URL=
SUPABASE_ANON_KEY=
SONIC_SOCIAL_ENABLED=false
# App Store build expects both buttons; keep both providers enabled in Supabase Auth.
SONIC_SOCIAL_PROVIDERS=google,apple

# Optional one-time helper for enabling Sign in with Apple in Supabase Auth.
# SUPABASE_ACCESS_TOKEN must be a Supabase Management API token with auth config write access.
SUPABASE_ACCESS_TOKEN=
SUPABASE_PROJECT_REF=
APPLE_SERVICE_ID=
# Either provide a generated Apple client secret:
APPLE_CLIENT_SECRET=
# Or let scripts/configure-supabase-apple-auth.mjs generate it from Apple Developer key material:
APPLE_TEAM_ID=
APPLE_KEY_ID=
APPLE_PRIVATE_KEY_PATH=

# Extra catalog reads published rows through /api/catalog-extra.
SONIC_CATALOG_EXTRA_ENABLED=true

# Owned API memory writes useful backend enrichments into catalog_enrichments.
# Requires the service role key and must stay server-side only.
SUPABASE_SERVICE_ROLE_KEY=
SONIC_CATALOG_ENRICHMENT_ENABLED=true
